{
  "name": "quill-image",
  "version": "0.0.2",
  "description": "A quill editor module for drop and paste image, with a callback hook before insert image into the editor",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && babel ./src/index.js --presets=es2015 > dist/index.js"
  },
  "keywords": [
    "quill",
    "editor",
    "drop",
    "image",
    "paste",
    "image"
  ],
  "author": "Adam Miller",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-env": "^1.2.2",
    "babel-preset-es2015": "^6.24.0"
  }
}
